# Blog Fase 1 — Fundação de dados + Blog público — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sair do placeholder "Em construção" em `/blog` para um blog público real — schema Supabase completo, `/blog` (listagem) e `/blog/[slug]` (detalhe), seguindo a identidade visual do portfólio.

**Architecture:** Schema Postgres no Supabase (6 tabelas, RLS, bucket de Storage) criado via MCP; camada de acesso a dados server-only em `src/modules/blog/data/`; componentes de apresentação em `src/modules/blog/components/`; duas rotas App Router (`/blog`, `/blog/[slug]`) como Server Components assíncronos.

**Tech Stack:** Next.js 16 (App Router, Server Components), `@supabase/ssr` / `supabase-js`, Tailwind CSS v4, shadcn/ui (`Badge`).

**Spec:** `docs/superpowers/specs/2026-09-03-blog-fase1-design.md` (e o roadmap em `docs/superpowers/specs/2026-09-03-blog-roadmap-design.md`)

## Global Constraints

- Sem suíte de testes automatizada neste repositório (decisão do scaffold original) — verificação via `pnpm lint`, `pnpm build`, queries SQL (`execute_sql`) e checagem manual no dev server (`pnpm dev` + `curl`).
- Projeto Supabase: nome `ecossistema-iris-blog`, ref `ptwvuamkioywkqdhzqcz`, região `sa-east-1`. Toda chamada MCP usa `project_id: "ptwvuamkioywkqdhzqcz"`.
- RLS habilitada em todas as tabelas de `public`; policies especificam papel via `TO anon` / `TO authenticated` explicitamente — nunca `auth.role() = 'authenticated'` (depreciado, quebra com login anônimo habilitado).
- DDL/RLS/triggers via `apply_migration` (MCP) — grava histórico de migration no projeto remoto. Leitura/verificação via `execute_sql` (MCP). Sem CLI local, sem stack Supabase local neste repo.
- Nenhum link para `/blog` é adicionado ao `SiteNav` do portfólio nesta fase — acesso só via URL direta.
- Paleta/tipografia do portfólio (`src/app/globals.css`): terracota `#C35A38` (hover `#A84728`), creme `#F3E6D3` / `#FAF4ED` / `#FCF7F0`, bordas `#E0CEB7`, texto `#291F1A` / muted `#6B5B52`, destaque `#F5D98C`. Títulos em Fraunces (`var(--font-fraunces)`), corpo em Montserrat (`var(--font-montserrat)`). Cards `rounded-2xl`.
- Conteúdo de post é HTML renderizado via `dangerouslySetInnerHTML`. Nesta fase só existe conteúdo de seed confiável (inserido por nós) — sanitização em tempo de escrita é requisito da Fase 2 (admin), não desta. Todo componente que renderiza `content` deve ter um comentário explicando essa premissa.

---

### Task 1: Schema do banco (tabelas, triggers, RLS)

**Files:** Nenhum arquivo local — mudanças aplicadas via MCP do Supabase (`apply_migration`, `execute_sql`) no projeto `ptwvuamkioywkqdhzqcz`.

**Interfaces:**
- Consumes: nada (primeira tarefa).
- Produces: tabelas `public.categories`, `public.tags`, `public.posts`, `public.post_tags`, `public.blog_settings`, `public.media_assets` com as colunas e RLS abaixo. Tarefas seguintes (Task 3 em diante) dependem desse schema existir exatamente como especificado.

- [ ] **Step 1: Aplicar migration — extensão, categorias e tags**

Chamar a tool MCP `apply_migration`:
- `project_id`: `"ptwvuamkioywkqdhzqcz"`
- `name`: `"create_categories_and_tags"`
- `query`:
```sql
create extension if not exists pgcrypto;

create table public.categories (
  id uuid primary key default gen_random_uuid(),
  slug text not null unique,
  name text not null,
  description text
);

alter table public.categories enable row level security;

create policy "public_read_categories"
on public.categories for select
to anon, authenticated
using (true);

create policy "authenticated_write_categories"
on public.categories for all
to authenticated
using (true)
with check (true);

create table public.tags (
  id uuid primary key default gen_random_uuid(),
  slug text not null unique,
  name text not null
);

alter table public.tags enable row level security;

create policy "public_read_tags"
on public.tags for select
to anon, authenticated
using (true);

create policy "authenticated_write_tags"
on public.tags for all
to authenticated
using (true)
with check (true);
```

- [ ] **Step 2: Verificar Step 1**

Chamar `execute_sql` com `project_id: "ptwvuamkioywkqdhzqcz"` e:
```sql
select tablename, rowsecurity
from pg_tables
where schemaname = 'public' and tablename in ('categories', 'tags');
```
Esperado: as duas linhas com `rowsecurity = true`.

- [ ] **Step 3: Aplicar migration — função de trigger e tabela `posts`**

`apply_migration`:
- `name`: `"create_posts_table"`
- `query`:
```sql
create or replace function public.set_updated_at()
returns trigger
language plpgsql
security invoker
set search_path = ''
as $$
begin
  new.updated_at = now();
  return new;
end;
$$;

create table public.posts (
  id uuid primary key default gen_random_uuid(),
  slug text not null unique,
  title text not null,
  excerpt text not null,
  content text not null,
  cover_image_url text,
  status text not null default 'draft' check (status in ('draft', 'published')),
  category_id uuid references public.categories(id) on delete set null,
  is_featured boolean not null default false,
  published_at timestamptz,
  seo_title text,
  seo_description text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create index posts_status_published_at_idx on public.posts (status, published_at desc);

alter table public.posts enable row level security;

create trigger set_posts_updated_at
before update on public.posts
for each row
execute function public.set_updated_at();

create policy "anon_read_published_posts"
on public.posts for select
to anon
using (status = 'published');

create policy "authenticated_read_all_posts"
on public.posts for select
to authenticated
using (true);

create policy "authenticated_insert_posts"
on public.posts for insert
to authenticated
with check (true);

create policy "authenticated_update_posts"
on public.posts for update
to authenticated
using (true)
with check (true);

create policy "authenticated_delete_posts"
on public.posts for delete
to authenticated
using (true);
```

- [ ] **Step 4: Verificar Step 3**

`execute_sql`:
```sql
select policyname, cmd, roles
from pg_policies
where schemaname = 'public' and tablename = 'posts'
order by policyname;
```
Esperado: 5 policies (`anon_read_published_posts`, `authenticated_read_all_posts`, `authenticated_insert_posts`, `authenticated_update_posts`, `authenticated_delete_posts`).

- [ ] **Step 5: Aplicar migration — `post_tags`**

`apply_migration`:
- `name`: `"create_post_tags_table"`
- `query`:
```sql
create table public.post_tags (
  post_id uuid not null references public.posts(id) on delete cascade,
  tag_id uuid not null references public.tags(id) on delete cascade,
  primary key (post_id, tag_id)
);

create index post_tags_tag_id_idx on public.post_tags (tag_id);

alter table public.post_tags enable row level security;

create policy "public_read_post_tags"
on public.post_tags for select
to anon, authenticated
using (true);

create policy "authenticated_write_post_tags"
on public.post_tags for all
to authenticated
using (true)
with check (true);
```

- [ ] **Step 6: Verificar Step 5**

`execute_sql`:
```sql
select count(*) as policy_count
from pg_policies
where schemaname = 'public' and tablename = 'post_tags';
```
Esperado: `policy_count = 2`.

- [ ] **Step 7: Aplicar migration — `blog_settings` (singleton) com linha padrão**

`apply_migration`:
- `name`: `"create_blog_settings_table"`
- `query`:
```sql
create table public.blog_settings (
  id smallint primary key default 1 check (id = 1),
  blog_title text not null default 'Blog',
  blog_description text,
  default_seo_image_url text,
  updated_at timestamptz not null default now()
);

alter table public.blog_settings enable row level security;

create trigger set_blog_settings_updated_at
before update on public.blog_settings
for each row
execute function public.set_updated_at();

create policy "public_read_blog_settings"
on public.blog_settings for select
to anon, authenticated
using (true);

create policy "authenticated_write_blog_settings"
on public.blog_settings for all
to authenticated
using (true)
with check (true);

insert into public.blog_settings (id, blog_title, blog_description)
values (1, 'Blog', 'Reflexões e bastidores da prática clínica e da mentoria.')
on conflict (id) do nothing;
```

- [ ] **Step 8: Verificar Step 7**

`execute_sql`:
```sql
select id, blog_title, blog_description from public.blog_settings;
```
Esperado: uma linha, `id = 1`, `blog_title = 'Blog'`.

- [ ] **Step 9: Aplicar migration — `media_assets`**

`apply_migration`:
- `name`: `"create_media_assets_table"`
- `query`:
```sql
create table public.media_assets (
  id uuid primary key default gen_random_uuid(),
  storage_path text not null unique,
  alt_text text,
  uploaded_at timestamptz not null default now()
);

alter table public.media_assets enable row level security;

create policy "authenticated_all_media_assets"
on public.media_assets for all
to authenticated
using (true)
with check (true);
```

- [ ] **Step 10: Verificação final do schema**

Chamar `list_tables` com `project_id: "ptwvuamkioywkqdhzqcz"`, `schemas: ["public"]`, `verbose: true`. Confirmar visualmente as 6 tabelas com as colunas esperadas (comparar com esta task).

Chamar `get_advisors` com `project_id: "ptwvuamkioywkqdhzqcz"`, `type: "security"`. Corrigir qualquer alerta antes de seguir — não deve haver nenhuma tabela em `public` sem RLS habilitada.

Sem commit local nesta task (nenhum arquivo do repositório foi alterado); o histórico fica registrado nas migrations do Supabase — confirmável via `list_migrations` (`project_id: "ptwvuamkioywkqdhzqcz"`), que deve listar as 5 migrations desta task.

---

### Task 2: Storage bucket `blog-media`

**Files:** Nenhum arquivo local — mudanças via MCP.

**Interfaces:**
- Consumes: nada (independente do schema de tabelas, mas documentado depois da Task 1 por organização).
- Produces: bucket `blog-media` (leitura pública) e policies em `storage.objects` restringindo escrita a `authenticated`. Usado pela Fase 2 (biblioteca de mídia) — nenhuma task desta fase consome o bucket ainda.

- [ ] **Step 1: Aplicar migration — bucket e policies**

`apply_migration`:
- `name`: `"create_blog_media_bucket"`
- `query`:
```sql
insert into storage.buckets (id, name, public)
values ('blog-media', 'blog-media', true)
on conflict (id) do nothing;

create policy "public_read_blog_media"
on storage.objects for select
to anon, authenticated
using (bucket_id = 'blog-media');

create policy "authenticated_insert_blog_media"
on storage.objects for insert
to authenticated
with check (bucket_id = 'blog-media');

create policy "authenticated_update_blog_media"
on storage.objects for update
to authenticated
using (bucket_id = 'blog-media')
with check (bucket_id = 'blog-media');

create policy "authenticated_delete_blog_media"
on storage.objects for delete
to authenticated
using (bucket_id = 'blog-media');
```

- [ ] **Step 2: Verificar**

`execute_sql`:
```sql
select id, public from storage.buckets where id = 'blog-media';
```
Esperado: `public = true`.

```sql
select policyname, cmd
from pg_policies
where schemaname = 'storage' and tablename = 'objects' and policyname like '%blog_media%'
order by policyname;
```
Esperado: 4 policies (select, insert, update, delete).

`get_advisors` (`type: "security"`) novamente — confirmar que o bucket público não gera alerta inesperado (bucket público de leitura é intencional, ver spec).

Sem commit local. Verificar com `list_migrations` que a migration `create_blog_media_bucket` aparece.

---

### Task 3: Seed de desenvolvimento

**Files:** Nenhum arquivo local — dados inseridos via MCP `execute_sql` (não é DDL, não entra no histórico de migrations — é dado de desenvolvimento, não schema).

**Interfaces:**
- Consumes: tabelas `posts`, `categories`, `tags`, `post_tags` da Task 1.
- Produces: 2 categorias (`reflexoes`, `bastidores`), 3 tags (`psicologia`, `mentoria`, `estudos`), 3 posts — `primeiro-post-do-blog` (publicado, destaque, categoria `reflexoes`, tags `psicologia`+`mentoria`), `segundo-post-sem-capa` (publicado, sem categoria/capa), `rascunho-de-teste` (rascunho). Tasks 6 e 7 usam esses slugs para verificação manual.

- [ ] **Step 1: Inserir categorias e tags**

`execute_sql`:
```sql
insert into public.categories (slug, name, description) values
  ('reflexoes', 'Reflexões', 'Pensamentos e reflexões pessoais.'),
  ('bastidores', 'Bastidores', 'Bastidores da prática clínica e da mentoria.')
on conflict (slug) do nothing;

insert into public.tags (slug, name) values
  ('psicologia', 'Psicologia'),
  ('mentoria', 'Mentoria'),
  ('estudos', 'Estudos')
on conflict (slug) do nothing;
```

- [ ] **Step 2: Inserir os 3 posts de exemplo**

`execute_sql`:
```sql
insert into public.posts (slug, title, excerpt, content, status, category_id, is_featured, published_at, seo_title, seo_description)
values (
  'primeiro-post-do-blog',
  'Primeiro post do blog',
  'Um texto de exemplo para testar a listagem e a página de post.',
  '<p>Este é um post de exemplo, inserido diretamente no banco para testar o blog antes da área de admin existir.</p><h2>Um subtítulo</h2><p>E mais um parágrafo de exemplo, com <a href="/blog">um link</a> só para checar o estilo.</p>',
  'published',
  (select id from public.categories where slug = 'reflexoes'),
  true,
  now() - interval '2 days',
  'Primeiro post do blog',
  'Um texto de exemplo para testar a listagem e a página de post.'
)
on conflict (slug) do nothing;

insert into public.post_tags (post_id, tag_id)
select p.id, t.id
from public.posts p, public.tags t
where p.slug = 'primeiro-post-do-blog' and t.slug in ('psicologia', 'mentoria')
on conflict do nothing;

insert into public.posts (slug, title, excerpt, content, status, is_featured, published_at)
values (
  'segundo-post-sem-capa',
  'Segundo post, sem imagem de capa',
  'Testando o card e a página de post sem cover_image_url preenchido.',
  '<p>Este post não tem capa nem categoria, só para garantir que a interface lida bem com esses campos vazios.</p>',
  'published',
  false,
  now() - interval '1 day'
)
on conflict (slug) do nothing;

insert into public.posts (slug, title, excerpt, content, status, is_featured)
values (
  'rascunho-de-teste',
  'Rascunho de teste',
  'Este post é um rascunho e não deve aparecer no blog público.',
  '<p>Conteúdo de rascunho.</p>',
  'draft',
  false
)
on conflict (slug) do nothing;
```

- [ ] **Step 3: Verificar contagem via `execute_sql`**

```sql
select status, count(*) from public.posts group by status order by status;
```
Esperado: `draft = 1`, `published = 2`.

- [ ] **Step 4: Verificar RLS de fora — chamada REST direta com a chave `anon`**

```bash
curl -s "https://ptwvuamkioywkqdhzqcz.supabase.co/rest/v1/posts?select=slug,status&order=slug.asc" \
  -H "apikey: sb_publishable_gUcZaSYzzSfjumppJoQlJA_z3mS8Rmb" \
  -H "Authorization: Bearer sb_publishable_gUcZaSYzzSfjumppJoQlJA_z3mS8Rmb"
```
Esperado: array JSON com exatamente `primeiro-post-do-blog` e `segundo-post-sem-capa` (2 itens, ambos `"status":"published"`) — `rascunho-de-teste` **não** deve aparecer. Se aparecer, a policy `anon_read_published_posts` da Task 1 está incorreta — pare e revise antes de continuar.

Sem commit local (dado de desenvolvimento, não versionado no git).

---

### Task 4: Tipos TypeScript (Supabase + domínio do blog)

**Files:**
- Modify: `src/lib/supabase/types.ts`
- Modify: `src/modules/blog/types/blog.types.ts`

**Interfaces:**
- Consumes: schema real criado na Task 1 (lido via introspecção do banco).
- Produces: tipo `Database` real em `src/lib/supabase/types.ts`; tipos de domínio `Category`, `Tag`, `PostSummary`, `PostDetail`, `BlogSettings` em `src/modules/blog/types/blog.types.ts` — consumidos pela Task 5 em diante.

- [ ] **Step 1: Gerar os tipos do Supabase**

Chamar a tool MCP `generate_typescript_types` com `project_id: "ptwvuamkioywkqdhzqcz"`. Copiar o conteúdo TypeScript retornado.

- [ ] **Step 2: Substituir `src/lib/supabase/types.ts`**

Substituir todo o conteúdo do arquivo (que hoje é o placeholder comentado) pelo conteúdo exato retornado no Step 1 — sem edições manuais.

- [ ] **Step 3: Reescrever `src/modules/blog/types/blog.types.ts`**

Substituir todo o conteúdo do arquivo por:
```typescript
export interface Category {
  id: string
  slug: string
  name: string
  description: string | null
}

export interface Tag {
  id: string
  slug: string
  name: string
}

export interface PostSummary {
  id: string
  slug: string
  title: string
  excerpt: string
  coverImageUrl: string | null
  isFeatured: boolean
  publishedAt: string | null
  category: Category | null
  tags: Tag[]
}

export interface PostDetail extends PostSummary {
  content: string
  seoTitle: string | null
  seoDescription: string | null
}

export interface BlogSettings {
  blogTitle: string
  blogDescription: string | null
  defaultSeoImageUrl: string | null
}
```

- [ ] **Step 4: Verificar**

Rodar: `pnpm exec tsc --noEmit`
Esperado: sem erros. Abrir `src/lib/supabase/types.ts` e confirmar que `Tables` inclui `posts`, `categories`, `tags`, `post_tags`, `blog_settings`, `media_assets` (não é mais o placeholder `Record<string, never>`).

- [ ] **Step 5: Commit**

```bash
git add src/lib/supabase/types.ts src/modules/blog/types/blog.types.ts
git commit -m "feat(blog): tipos reais do Supabase e do domínio do blog"
```

---

### Task 5: Camada de acesso a dados

**Files:**
- Create: `src/modules/blog/data/posts.data.ts`

**Interfaces:**
- Consumes: `createClient` de `@/lib/supabase/server` (assíncrono); tipos `Category`, `Tag`, `PostSummary`, `PostDetail`, `BlogSettings` de `@/modules/blog/types/blog.types` (Task 4).
- Produces: `getPublishedPosts(params?: PostListParams): Promise<PostListResult>`, `getPostBySlug(slug: string): Promise<PostDetail | null>`, `getCategories(): Promise<Category[]>`, `getTags(): Promise<Tag[]>`, `getBlogSettings(): Promise<BlogSettings>`, e os tipos `PostListParams { categorySlug?: string; tagSlug?: string; page?: number; pageSize?: number }` / `PostListResult { posts: PostSummary[]; totalCount: number; page: number; pageSize: number }`. Consumidos pelas Tasks 6 e 7.

- [ ] **Step 1: Criar o arquivo**

Criar `src/modules/blog/data/posts.data.ts`:
```typescript
import 'server-only'
import { createClient } from '@/lib/supabase/server'
import type {
  BlogSettings,
  Category,
  PostDetail,
  PostSummary,
  Tag,
} from '@/modules/blog/types/blog.types'

type CategoryRow = {
  id: string
  slug: string
  name: string
  description: string | null
}

type TagRow = {
  id: string
  slug: string
  name: string
}

type PostSummaryRow = {
  id: string
  slug: string
  title: string
  excerpt: string
  cover_image_url: string | null
  is_featured: boolean
  published_at: string | null
  category: CategoryRow | null
  post_tags: { tag: TagRow }[]
}

type PostDetailRow = PostSummaryRow & {
  content: string
  seo_title: string | null
  seo_description: string | null
}

// O tipo gerado pelo supabase-js para selects com embeds aninhados
// (category:categories(...), post_tags(tag:tags(...))) não reflete com
// precisão o formato real da resposta — por isso o cast explícito para
// os Row types acima, definidos a partir das colunas realmente pedidas.
const POST_SUMMARY_COLUMNS =
  'id, slug, title, excerpt, cover_image_url, is_featured, published_at, category:categories(id, slug, name, description), post_tags(tag:tags(id, slug, name))'

const POST_DETAIL_COLUMNS = `${POST_SUMMARY_COLUMNS}, content, seo_title, seo_description`

function mapPostSummary(row: PostSummaryRow): PostSummary {
  return {
    id: row.id,
    slug: row.slug,
    title: row.title,
    excerpt: row.excerpt,
    coverImageUrl: row.cover_image_url,
    isFeatured: row.is_featured,
    publishedAt: row.published_at,
    category: row.category,
    tags: row.post_tags.map((postTag) => postTag.tag),
  }
}

function mapPostDetail(row: PostDetailRow): PostDetail {
  return {
    ...mapPostSummary(row),
    content: row.content,
    seoTitle: row.seo_title,
    seoDescription: row.seo_description,
  }
}

export interface PostListParams {
  categorySlug?: string
  tagSlug?: string
  page?: number
  pageSize?: number
}

export interface PostListResult {
  posts: PostSummary[]
  totalCount: number
  page: number
  pageSize: number
}

export async function getPublishedPosts(
  params: PostListParams = {}
): Promise<PostListResult> {
  const supabase = await createClient()
  const page = params.page && params.page > 0 ? params.page : 1
  const pageSize = params.pageSize ?? 9

  let postIdFilter: string[] | null = null

  if (params.tagSlug) {
    const { data: tag } = await supabase
      .from('tags')
      .select('id')
      .eq('slug', params.tagSlug)
      .maybeSingle()

    if (!tag) {
      return { posts: [], totalCount: 0, page, pageSize }
    }

    const { data: postTags } = await supabase
      .from('post_tags')
      .select('post_id')
      .eq('tag_id', tag.id)

    postIdFilter = (postTags ?? []).map((row) => row.post_id)

    if (postIdFilter.length === 0) {
      return { posts: [], totalCount: 0, page, pageSize }
    }
  }

  let query = supabase
    .from('posts')
    .select(POST_SUMMARY_COLUMNS, { count: 'exact' })
    .eq('status', 'published')
    .order('published_at', { ascending: false })

  if (params.categorySlug) {
    const { data: category } = await supabase
      .from('categories')
      .select('id')
      .eq('slug', params.categorySlug)
      .maybeSingle()

    if (!category) {
      return { posts: [], totalCount: 0, page, pageSize }
    }

    query = query.eq('category_id', category.id)
  }

  if (postIdFilter) {
    query = query.in('id', postIdFilter)
  }

  const { data, error, count } = await query.range(
    (page - 1) * pageSize,
    page * pageSize - 1
  )

  if (error) {
    throw new Error(`Failed to load published posts: ${error.message}`)
  }

  return {
    posts: (data ?? []).map((row) =>
      mapPostSummary(row as unknown as PostSummaryRow)
    ),
    totalCount: count ?? 0,
    page,
    pageSize,
  }
}

export async function getPostBySlug(slug: string): Promise<PostDetail | null> {
  const supabase = await createClient()

  const { data, error } = await supabase
    .from('posts')
    .select(POST_DETAIL_COLUMNS)
    .eq('slug', slug)
    .eq('status', 'published')
    .maybeSingle()

  if (error) {
    throw new Error(`Failed to load post "${slug}": ${error.message}`)
  }

  return data ? mapPostDetail(data as unknown as PostDetailRow) : null
}

export async function getCategories(): Promise<Category[]> {
  const supabase = await createClient()
  const { data, error } = await supabase
    .from('categories')
    .select('id, slug, name, description')
    .order('name', { ascending: true })

  if (error) {
    throw new Error(`Failed to load categories: ${error.message}`)
  }

  return data ?? []
}

export async function getTags(): Promise<Tag[]> {
  const supabase = await createClient()
  const { data, error } = await supabase
    .from('tags')
    .select('id, slug, name')
    .order('name', { ascending: true })

  if (error) {
    throw new Error(`Failed to load tags: ${error.message}`)
  }

  return data ?? []
}

export async function getBlogSettings(): Promise<BlogSettings> {
  const supabase = await createClient()
  const { data, error } = await supabase
    .from('blog_settings')
    .select('blog_title, blog_description, default_seo_image_url')
    .eq('id', 1)
    .single()

  if (error) {
    throw new Error(`Failed to load blog settings: ${error.message}`)
  }

  return {
    blogTitle: data.blog_title,
    blogDescription: data.blog_description,
    defaultSeoImageUrl: data.default_seo_image_url,
  }
}
```

- [ ] **Step 2: Verificar tipos e lint**

Rodar: `pnpm exec tsc --noEmit && pnpm lint`
Esperado: sem erros. Este arquivo ainda não é consumido por nenhuma página — a verificação de comportamento em runtime acontece nas Tasks 6 e 7, quando as páginas o chamam de fato.

- [ ] **Step 3: Commit**

```bash
git add src/modules/blog/data/posts.data.ts
git commit -m "feat(blog): camada de acesso a dados do blog"
```

---

### Task 6: Página `/blog` (listagem)

**Files:**
- Create: `src/modules/blog/components/blog-nav.tsx`
- Create: `src/modules/blog/components/category-badge.tsx`
- Create: `src/modules/blog/components/tag-badge.tsx`
- Create: `src/modules/blog/components/post-card.tsx`
- Create: `src/modules/blog/components/post-list.tsx`
- Modify: `src/app/blog/page.tsx` (remove o placeholder "Em construção")
- Delete: `src/modules/blog/components/.gitkeep` (não é mais uma pasta vazia)

**Interfaces:**
- Consumes: `getBlogSettings`, `getCategories`, `getTags`, `getPublishedPosts`, `PostListParams`, `PostListResult` de `@/modules/blog/data/posts.data` (Task 5); `Category`, `Tag`, `PostSummary`, `BlogSettings` de `@/modules/blog/types/blog.types` (Task 4); `Badge` de `@/components/ui/badge`.
- Produces: `BlogNav`, `CategoryBadge`, `TagBadge`, `PostCard`, `PostList` — `BlogNav`, `CategoryBadge` e `TagBadge` são reaproveitados pela Task 7.

- [ ] **Step 1: Criar `blog-nav.tsx`**

```tsx
import Link from 'next/link'

export function BlogNav({ blogTitle }: { blogTitle: string }) {
  return (
    <header className="sticky top-0 z-50 border-b border-[#E0CEB7] bg-[#F3E6D3]/95 backdrop-blur-md">
      <nav className="mx-auto flex max-w-6xl items-center justify-between gap-3 px-4 py-3.5 sm:px-6 sm:py-4">
        <Link
          href="/blog"
          className="font-display text-xl font-bold tracking-tight text-[#291F1A] transition-colors hover:text-[#C35A38] sm:text-2xl"
        >
          {blogTitle}
        </Link>
        <Link
          href="/portfolio"
          className="text-sm font-medium text-[#6B5B52] transition-colors hover:text-[#291F1A]"
        >
          ← Voltar ao portfólio
        </Link>
      </nav>
    </header>
  )
}
```

- [ ] **Step 2: Criar `category-badge.tsx` e `tag-badge.tsx`**

```tsx
// src/modules/blog/components/category-badge.tsx
import { Badge } from '@/components/ui/badge'
import type { Category } from '@/modules/blog/types/blog.types'

export function CategoryBadge({ category }: { category: Category }) {
  return (
    <Badge className="bg-[#C35A38] text-[#FAF4ED] hover:bg-[#A84728]">
      {category.name}
    </Badge>
  )
}
```

```tsx
// src/modules/blog/components/tag-badge.tsx
import { Badge } from '@/components/ui/badge'
import type { Tag } from '@/modules/blog/types/blog.types'

export function TagBadge({ tag }: { tag: Tag }) {
  return (
    <Badge variant="outline" className="border-[#E0CEB7] text-[#6B5B52]">
      #{tag.name}
    </Badge>
  )
}
```

- [ ] **Step 3: Criar `post-card.tsx`**

```tsx
import Link from 'next/link'
import type { PostSummary } from '@/modules/blog/types/blog.types'
import { CategoryBadge } from './category-badge'
import { TagBadge } from './tag-badge'

export function PostCard({ post }: { post: PostSummary }) {
  return (
    <article className="flex flex-col overflow-hidden rounded-2xl border border-[#E0CEB7] bg-[#FCF7F0] shadow-sm transition-shadow hover:shadow-md">
      {post.coverImageUrl && (
        <Link
          href={`/blog/${post.slug}`}
          className="block aspect-[16/9] overflow-hidden"
        >
          {/* next/image fica para a Fase 3 (otimização de imagens) */}
          {/* eslint-disable-next-line @next/next/no-img-element */}
          <img
            src={post.coverImageUrl}
            alt={post.title}
            className="h-full w-full object-cover transition-transform duration-300 hover:scale-105"
          />
        </Link>
      )}
      <div className="flex flex-1 flex-col gap-3 p-5 sm:p-6">
        <div className="flex flex-wrap items-center gap-2">
          {post.category && <CategoryBadge category={post.category} />}
          {post.isFeatured && (
            <span className="rounded-full bg-[#F5D98C] px-3 py-1 text-xs font-semibold uppercase tracking-wide text-[#291F1A]">
              Destaque
            </span>
          )}
        </div>
        <h2 className="font-display text-xl font-bold tracking-tight text-[#291F1A]">
          <Link
            href={`/blog/${post.slug}`}
            className="transition-colors hover:text-[#C35A38]"
          >
            {post.title}
          </Link>
        </h2>
        <p className="text-sm text-[#6B5B52]">{post.excerpt}</p>
        {post.tags.length > 0 && (
          <div className="flex flex-wrap gap-2 pt-1">
            {post.tags.map((tag) => (
              <TagBadge key={tag.id} tag={tag} />
            ))}
          </div>
        )}
      </div>
    </article>
  )
}
```

- [ ] **Step 4: Criar `post-list.tsx`**

```tsx
import type { PostSummary } from '@/modules/blog/types/blog.types'
import { PostCard } from './post-card'

export function PostList({ posts }: { posts: PostSummary[] }) {
  if (posts.length === 0) {
    return (
      <div className="rounded-2xl border border-dashed border-[#E0CEB7] bg-[#FAF4ED] px-6 py-16 text-center">
        <p className="font-display text-lg font-semibold text-[#291F1A]">
          Nenhum post por aqui ainda
        </p>
        <p className="mt-2 text-sm text-[#6B5B52]">
          Volte em breve para novos conteúdos.
        </p>
      </div>
    )
  }

  return (
    <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {posts.map((post) => (
        <PostCard key={post.id} post={post} />
      ))}
    </div>
  )
}
```

- [ ] **Step 5: Substituir `src/app/blog/page.tsx`**

```tsx
import type { Metadata } from 'next'
import Link from 'next/link'
import { BlogNav } from '@/modules/blog/components/blog-nav'
import { PostList } from '@/modules/blog/components/post-list'
import { CategoryBadge } from '@/modules/blog/components/category-badge'
import { TagBadge } from '@/modules/blog/components/tag-badge'
import {
  getBlogSettings,
  getCategories,
  getPublishedPosts,
  getTags,
} from '@/modules/blog/data/posts.data'

const PAGE_SIZE = 9

export async function generateMetadata(): Promise<Metadata> {
  const settings = await getBlogSettings()
  return {
    title: settings.blogTitle,
    description: settings.blogDescription ?? undefined,
  }
}

function buildPageHref(page: number, categorySlug?: string, tagSlug?: string) {
  const qs = new URLSearchParams()
  if (categorySlug) qs.set('categoria', categorySlug)
  if (tagSlug) qs.set('tag', tagSlug)
  qs.set('pagina', String(page))
  return `/blog?${qs.toString()}`
}

export default async function BlogPage({
  searchParams,
}: {
  searchParams: Promise<{ [key: string]: string | string[] | undefined }>
}) {
  const params = await searchParams
  const categorySlug =
    typeof params.categoria === 'string' ? params.categoria : undefined
  const tagSlug = typeof params.tag === 'string' ? params.tag : undefined
  const requestedPage =
    typeof params.pagina === 'string' ? Number(params.pagina) || 1 : 1

  const [settings, categories, tags, { posts, totalCount, page, pageSize }] =
    await Promise.all([
      getBlogSettings(),
      getCategories(),
      getTags(),
      getPublishedPosts({
        categorySlug,
        tagSlug,
        page: requestedPage,
        pageSize: PAGE_SIZE,
      }),
    ])

  const totalPages = Math.max(1, Math.ceil(totalCount / pageSize))
  const isFirstUnfilteredPage = page === 1 && !categorySlug && !tagSlug
  const featuredPost = isFirstUnfilteredPage
    ? posts.find((post) => post.isFeatured)
    : undefined
  const remainingPosts = featuredPost
    ? posts.filter((post) => post.id !== featuredPost.id)
    : posts

  return (
    <div className="min-h-screen bg-[#F3E6D3] text-[#291F1A]">
      <BlogNav blogTitle={settings.blogTitle} />
      <main className="mx-auto max-w-6xl px-4 py-12 sm:px-6 sm:py-16">
        <header className="mb-10 flex flex-col gap-3">
          <h1 className="font-display text-3xl font-bold tracking-tight sm:text-4xl">
            {settings.blogTitle}
          </h1>
          {settings.blogDescription && (
            <p className="max-w-2xl text-[#6B5B52]">
              {settings.blogDescription}
            </p>
          )}
        </header>

        {(categories.length > 0 || tags.length > 0) && (
          <div className="mb-10 flex flex-wrap items-center gap-2">
            {categories.map((category) => (
              <Link key={category.id} href={`/blog?categoria=${category.slug}`}>
                <CategoryBadge category={category} />
              </Link>
            ))}
            {tags.map((tag) => (
              <Link key={tag.id} href={`/blog?tag=${tag.slug}`}>
                <TagBadge tag={tag} />
              </Link>
            ))}
            {(categorySlug || tagSlug) && (
              <Link
                href="/blog"
                className="text-xs font-medium text-[#C35A38] underline underline-offset-2"
              >
                Limpar filtro
              </Link>
            )}
          </div>
        )}

        {featuredPost && (
          <section className="mb-12">
            <PostList posts={[featuredPost]} />
          </section>
        )}

        <PostList posts={remainingPosts} />

        {totalPages > 1 && (
          <nav className="mt-12 flex items-center justify-center gap-4 text-sm font-medium">
            {page > 1 && (
              <Link
                href={buildPageHref(page - 1, categorySlug, tagSlug)}
                className="rounded-full border border-[#E0CEB7] px-4 py-2 text-[#291F1A] transition-colors hover:border-[#C35A38] hover:text-[#C35A38]"
              >
                ← Anterior
              </Link>
            )}
            <span className="text-[#6B5B52]">
              Página {page} de {totalPages}
            </span>
            {page < totalPages && (
              <Link
                href={buildPageHref(page + 1, categorySlug, tagSlug)}
                className="rounded-full border border-[#E0CEB7] px-4 py-2 text-[#291F1A] transition-colors hover:border-[#C35A38] hover:text-[#C35A38]"
              >
                Próxima →
              </Link>
            )}
          </nav>
        )}
      </main>
    </div>
  )
}
```

- [ ] **Step 6: Remover o `.gitkeep`**

```bash
git rm src/modules/blog/components/.gitkeep
```

- [ ] **Step 7: Lint e build**

Rodar: `pnpm lint && pnpm build`
Esperado: sem erros. `/blog` usa `searchParams`, então é renderizada dinamicamente — o build deve compilar sem tentar pré-renderizar essa rota estaticamente.

- [ ] **Step 8: Checagem manual no dev server**

```bash
pnpm dev &
sleep 3
curl -s http://localhost:3000/blog | grep -o "Primeiro post do blog"
curl -s "http://localhost:3000/blog?categoria=reflexoes" | grep -o "Primeiro post do blog"
curl -s "http://localhost:3000/blog?categoria=inexistente" | grep -o "Nenhum post por aqui ainda"
kill %1
```
Esperado: os dois primeiros `grep` encontram o título; o terceiro confirma o estado vazio para uma categoria sem posts. Depois, abrir `http://localhost:3000/blog` no navegador e confirmar visualmente: cores terracota/creme, título em Fraunces, card do post em destaque, badges de categoria/tag.

- [ ] **Step 9: Commit**

```bash
git add src/modules/blog/components src/app/blog/page.tsx
git commit -m "feat(blog): página de listagem /blog"
```

---

### Task 7: Página `/blog/[slug]` (detalhe)

**Files:**
- Create: `src/modules/blog/components/post-body.tsx`
- Create: `src/app/blog/[slug]/page.tsx`
- Modify: `src/app/globals.css` (adiciona `.prose-blog`)
- Modify: `src/modules/blog/README.md` (remove o texto de placeholder, aponta para as specs)

**Interfaces:**
- Consumes: `getPostBySlug`, `getBlogSettings` de `@/modules/blog/data/posts.data` (Task 5); `BlogNav`, `CategoryBadge`, `TagBadge` de `@/modules/blog/components/*` (Task 6).
- Produces: `PostBody` (renderiza `content` HTML), rota `/blog/[slug]`.

- [ ] **Step 1: Criar `post-body.tsx`**

```tsx
// Conteúdo vem de seed confiável nesta fase (sem editor de usuário ainda).
// A Fase 2 introduz sanitização em tempo de escrita antes de qualquer HTML
// gerado por usuário chegar aqui — ver spec da Fase 1, seção "Riscos e
// observações".
export function PostBody({ html }: { html: string }) {
  return (
    <div className="prose-blog max-w-none" dangerouslySetInnerHTML={{ __html: html }} />
  )
}
```

- [ ] **Step 2: Adicionar estilos `.prose-blog` a `src/app/globals.css`**

Adicionar ao final do arquivo:
```css
@layer components {
  .prose-blog {
    color: #291f1a;
    line-height: 1.75;
  }
  .prose-blog h2 {
    font-family: var(--font-fraunces), Georgia, serif;
    font-size: 1.5rem;
    font-weight: 700;
    margin-top: 2.5rem;
    margin-bottom: 1rem;
  }
  .prose-blog h3 {
    font-family: var(--font-fraunces), Georgia, serif;
    font-size: 1.25rem;
    font-weight: 700;
    margin-top: 2rem;
    margin-bottom: 0.75rem;
  }
  .prose-blog p {
    margin-bottom: 1.25rem;
  }
  .prose-blog a {
    color: #c35a38;
    text-decoration: underline;
    text-underline-offset: 2px;
  }
  .prose-blog ul,
  .prose-blog ol {
    margin-bottom: 1.25rem;
    padding-left: 1.5rem;
  }
  .prose-blog ul {
    list-style-type: disc;
  }
  .prose-blog ol {
    list-style-type: decimal;
  }
  .prose-blog blockquote {
    border-left: 3px solid #c35a38;
    margin: 1.5rem 0;
    padding-left: 1rem;
    color: #6b5b52;
    font-style: italic;
  }
  .prose-blog img {
    margin: 1.5rem 0;
    border-radius: 1rem;
  }
}
```

- [ ] **Step 3: Criar `src/app/blog/[slug]/page.tsx`**

```tsx
import type { Metadata } from 'next'
import { notFound } from 'next/navigation'
import { BlogNav } from '@/modules/blog/components/blog-nav'
import { CategoryBadge } from '@/modules/blog/components/category-badge'
import { TagBadge } from '@/modules/blog/components/tag-badge'
import { PostBody } from '@/modules/blog/components/post-body'
import { getBlogSettings, getPostBySlug } from '@/modules/blog/data/posts.data'

export async function generateMetadata({
  params,
}: {
  params: Promise<{ slug: string }>
}): Promise<Metadata> {
  const { slug } = await params
  const post = await getPostBySlug(slug)

  if (!post) {
    return { title: 'Post não encontrado' }
  }

  return {
    title: post.seoTitle ?? post.title,
    description: post.seoDescription ?? post.excerpt,
    openGraph: {
      title: post.seoTitle ?? post.title,
      description: post.seoDescription ?? post.excerpt,
      images: post.coverImageUrl ? [post.coverImageUrl] : undefined,
    },
  }
}

export default async function BlogPostPage({
  params,
}: {
  params: Promise<{ slug: string }>
}) {
  const { slug } = await params
  const [post, settings] = await Promise.all([
    getPostBySlug(slug),
    getBlogSettings(),
  ])

  if (!post) {
    notFound()
  }

  return (
    <div className="min-h-screen bg-[#F3E6D3] text-[#291F1A]">
      <BlogNav blogTitle={settings.blogTitle} />
      <main className="mx-auto max-w-3xl px-4 py-12 sm:px-6 sm:py-16">
        <article>
          <header className="mb-8 flex flex-col gap-4">
            <div className="flex flex-wrap items-center gap-2">
              {post.category && <CategoryBadge category={post.category} />}
              {post.tags.map((tag) => (
                <TagBadge key={tag.id} tag={tag} />
              ))}
            </div>
            <h1 className="font-display text-3xl font-bold tracking-tight sm:text-4xl">
              {post.title}
            </h1>
            {post.publishedAt && (
              <time dateTime={post.publishedAt} className="text-sm text-[#6B5B52]">
                {new Date(post.publishedAt).toLocaleDateString('pt-BR', {
                  day: '2-digit',
                  month: 'long',
                  year: 'numeric',
                })}
              </time>
            )}
            {post.coverImageUrl && (
              // eslint-disable-next-line @next/next/no-img-element
              <img
                src={post.coverImageUrl}
                alt={post.title}
                className="mt-2 aspect-[16/9] w-full rounded-2xl object-cover"
              />
            )}
          </header>
          <PostBody html={post.content} />
        </article>
      </main>
    </div>
  )
}
```

- [ ] **Step 4: Atualizar `src/modules/blog/README.md`**

Substituir todo o conteúdo por:
```markdown
# Módulo de Blog

Blog público implementado (Fase 1 do roadmap): `/blog` (listagem) e
`/blog/[slug]` (detalhe), servidos por um schema Supabase real.

Ver:
- `docs/superpowers/specs/2026-09-03-blog-roadmap-design.md` — roadmap completo (3 fases)
- `docs/superpowers/specs/2026-09-03-blog-fase1-design.md` — spec desta fase
- `docs/superpowers/plans/2026-09-03-blog-fase1.md` — plano de implementação

Área de admin (Fase 2) e hardening/SEO (Fase 3) ainda não implementados.
```

- [ ] **Step 5: Lint e build completos**

Rodar: `pnpm lint && pnpm exec tsc --noEmit && pnpm build`
Esperado: sem erros.

- [ ] **Step 6: Checagem manual no dev server**

```bash
pnpm dev &
sleep 3
curl -s http://localhost:3000/blog/primeiro-post-do-blog | grep -o "Primeiro post do blog"
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/blog/slug-que-nao-existe
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/blog/rascunho-de-teste
kill %1
```
Esperado: o primeiro `grep` encontra o título; os dois códigos HTTP seguintes são `404` (slug inexistente **e** o rascunho — RLS bloqueia o rascunho para `anon`, então `getPostBySlug` retorna `null` e a página chama `notFound()`). Depois, abrir `http://localhost:3000/blog/primeiro-post-do-blog` no navegador e confirmar visualmente: título em Fraunces, badges, data formatada em pt-BR, corpo do post com `<h2>` e link estilizados pelo `.prose-blog`.

- [ ] **Step 7: Commit**

```bash
git add src/modules/blog/components/post-body.tsx src/app/blog/[slug]/page.tsx src/app/globals.css src/modules/blog/README.md
git commit -m "feat(blog): página de detalhe /blog/[slug]"
```
