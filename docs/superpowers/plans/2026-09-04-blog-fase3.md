# Blog Fase 3 — Hardening, SEO e Produção Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Preparar o blog para produção — sitemap/robots, fallback de imagem de SEO, dados estruturados (JSON-LD), `next/image` nas páginas públicas, estados de erro/carregamento, e um checklist de deploy — fechando o roadmap original de 3 fases.

**Architecture:** Mesmo padrão das fases anteriores (Server Components buscando dados, `src/lib/` para utilitários compartilhados). Introduz `src/lib/site-url.ts` (URL absoluta do site, usada por `sitemap.ts`, `robots.ts` e `metadataBase`) e um campo novo (`updatedAt`) em `PostSummary`, consumido tanto pelo sitemap quanto pelo JSON-LD.

**Tech Stack:** Next.js 16 (App Router — convenções `sitemap.ts`/`robots.ts`/`loading.tsx`/`not-found.tsx`), `next/image`, Supabase (sem migration nesta fase).

**Spec:** `docs/superpowers/specs/2026-09-04-blog-fase3-design.md` (depende de `docs/superpowers/specs/2026-09-03-blog-roadmap-design.md`, `docs/superpowers/specs/2026-09-03-blog-fase1-design.md`, `docs/superpowers/specs/2026-09-03-blog-fase2a-design.md`, `docs/superpowers/specs/2026-09-03-blog-fase2b-design.md`)

## Global Constraints

- Sem suíte de testes automatizada — verificação via `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `curl` (só GET/redirecionamento).
- Sem migration de schema nesta fase — todas as tabelas e o bucket já existem desde a Fase 1.
- Paleta terracota/creme (`#F3E6D3`, `#E0CEB7`, `#291F1A`, `#6B5B52`, `#C35A38`, `#FCF7F0`) nas páginas públicas do blog; paleta cinza neutra (`gray-*`) no admin — mesma distinção já estabelecida nas fases anteriores.
- `next/image` só nas páginas públicas do blog (`PostCard`, `/blog/[slug]`) — a biblioteca de mídia do admin continua com `<img>` simples (thumbnails internos, sem benefício de SEO/performance pro visitante).
- Autora única e fixa ("Íris") — hardcoded no JSON-LD, sem novo campo de autor no schema.
- A auditoria de RLS já foi feita durante o brainstorming desta fase (ver spec, seção "Auditoria de RLS") — nenhuma task deste plano repete a consulta ao banco.

---

### Task 1: `NEXT_PUBLIC_SITE_URL` + `metadataBase`

**Files:**
- Modify: `.env.example`
- Modify: `.env.local` (não versionado — comando local, não um diff)
- Create: `src/lib/site-url.ts`
- Modify: `src/app/layout.tsx`

**Interfaces:**
- Produces: `getSiteUrl(): string` — usado pelas Tasks 3 (sitemap/robots) e 4 (JSON-LD).

- [ ] **Step 1: Adicionar a variável em `.env.example`**

```bash
cat >> .env.example << 'EOF'
NEXT_PUBLIC_SITE_URL=
EOF
```

Conteúdo final esperado de `.env.example`:

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
ADMIN_USER_ID=
NEXT_PUBLIC_SITE_URL=
```

- [ ] **Step 2: Adicionar o valor de desenvolvimento em `.env.local`**

```bash
cat >> .env.local << 'EOF'
NEXT_PUBLIC_SITE_URL=http://localhost:3000
EOF
```

`.env.local` é ignorado pelo git — este passo não aparece em nenhum commit, só prepara o ambiente local para os passos de verificação das próximas tasks (sitemap, robots).

- [ ] **Step 3: Criar `src/lib/site-url.ts`**

```typescript
export function getSiteUrl(): string {
  return process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000'
}
```

- [ ] **Step 4: Substituir `src/app/layout.tsx` inteiro**

```tsx
import type { Metadata } from 'next'
import { Fraunces, Montserrat, Caveat } from 'next/font/google'
import './globals.css'
import { getSiteUrl } from '@/lib/site-url'

const fraunces = Fraunces({
  variable: '--font-fraunces',
  subsets: ['latin'],
  display: 'swap',
})

const montserrat = Montserrat({
  variable: '--font-montserrat',
  subsets: ['latin'],
  display: 'swap',
})

const caveat = Caveat({
  variable: '--font-caveat',
  subsets: ['latin'],
  display: 'swap',
})

export const metadata: Metadata = {
  metadataBase: new URL(getSiteUrl()),
  title: 'Íris — Psicologia Clínica & Mentoria para Estudantes',
  description:
    'Acolhimento, supervisão e mentoria prática para estudantes de Psicologia construírem sua prática clínica com segurança, ética e afeto.',
}


export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html
      lang="pt-BR"
      className={`${fraunces.variable} ${montserrat.variable} ${caveat.variable} scroll-smooth`}
    >
      <body className="font-sans antialiased text-[#291F1A] bg-[#F3E6D3] selection:bg-[#E8A76F] selection:text-[#291F1A]">
        {children}
      </body>
    </html>
  )
}
```

- [ ] **Step 5: Verificar**

```bash
pnpm exec tsc --noEmit && pnpm lint && pnpm build
```
Esperado: sem erros. `metadataBase` não muda nenhuma página visualmente — é só a base para resolver URLs relativas de metadata (usado a partir da Task 4).

- [ ] **Step 6: Commit**

```bash
git add .env.example src/lib/site-url.ts src/app/layout.tsx
git commit -m "feat(seo): NEXT_PUBLIC_SITE_URL e metadataBase"
```

---

### Task 2: Expor `updatedAt` em `PostSummary`

**Files:**
- Modify: `src/modules/blog/types/blog.types.ts` (substituição completa do arquivo)
- Modify: `src/modules/blog/data/posts.data.ts` (substituição completa do arquivo)

**Interfaces:**
- Consumes: nada novo — só expõe uma coluna (`posts.updated_at`) que já existe na tabela desde a Fase 1.
- Produces: `PostSummary.updatedAt: string` (e, por extensão, `PostDetail.updatedAt`) — consumido pela Task 3 (sitemap) e Task 4 (JSON-LD `dateModified`).

- [ ] **Step 1: Substituir `src/modules/blog/types/blog.types.ts` inteiro**

```typescript
export interface Category {
  id: string
  slug: string
  name: string
  description: string | null
}

export interface Tag {
  id: string
  slug: string
  name: string
}

export interface PostSummary {
  id: string
  slug: string
  title: string
  excerpt: string
  coverImageUrl: string | null
  isFeatured: boolean
  publishedAt: string | null
  updatedAt: string
  category: Category | null
  tags: Tag[]
}

export interface PostDetail extends PostSummary {
  content: string
  seoTitle: string | null
  seoDescription: string | null
}

export interface BlogSettings {
  blogTitle: string
  blogDescription: string | null
  defaultSeoImageUrl: string | null
}
```

- [ ] **Step 2: Substituir `src/modules/blog/data/posts.data.ts` inteiro**

```typescript
import 'server-only'
import { createClient } from '@/lib/supabase/server'
import type {
  BlogSettings,
  Category,
  PostDetail,
  PostSummary,
  Tag,
} from '@/modules/blog/types/blog.types'

type CategoryRow = {
  id: string
  slug: string
  name: string
  description: string | null
}

type TagRow = {
  id: string
  slug: string
  name: string
}

type PostSummaryRow = {
  id: string
  slug: string
  title: string
  excerpt: string
  cover_image_url: string | null
  is_featured: boolean
  published_at: string | null
  updated_at: string
  category: CategoryRow | null
  post_tags: { tag: TagRow }[]
}

type PostDetailRow = PostSummaryRow & {
  content: string
  seo_title: string | null
  seo_description: string | null
}

// O tipo gerado pelo supabase-js para selects com embeds aninhados
// (category:categories(...), post_tags(tag:tags(...))) não reflete com
// precisão o formato real da resposta — por isso o cast explícito para
// os Row types acima, definidos a partir das colunas realmente pedidas.
const POST_SUMMARY_COLUMNS =
  'id, slug, title, excerpt, cover_image_url, is_featured, published_at, updated_at, category:categories(id, slug, name, description), post_tags(tag:tags(id, slug, name))'

const POST_DETAIL_COLUMNS = `${POST_SUMMARY_COLUMNS}, content, seo_title, seo_description`

function mapPostSummary(row: PostSummaryRow): PostSummary {
  return {
    id: row.id,
    slug: row.slug,
    title: row.title,
    excerpt: row.excerpt,
    coverImageUrl: row.cover_image_url,
    isFeatured: row.is_featured,
    publishedAt: row.published_at,
    updatedAt: row.updated_at,
    category: row.category,
    tags: row.post_tags.map((postTag) => postTag.tag),
  }
}

function mapPostDetail(row: PostDetailRow): PostDetail {
  return {
    ...mapPostSummary(row),
    content: row.content,
    seoTitle: row.seo_title,
    seoDescription: row.seo_description,
  }
}

export interface PostListParams {
  categorySlug?: string
  tagSlug?: string
  page?: number
  pageSize?: number
}

export interface PostListResult {
  posts: PostSummary[]
  totalCount: number
  page: number
  pageSize: number
}

export async function getPublishedPosts(
  params: PostListParams = {}
): Promise<PostListResult> {
  const supabase = await createClient()
  const page = params.page && params.page > 0 ? params.page : 1
  const pageSize = params.pageSize ?? 9

  let postIdFilter: string[] | null = null

  if (params.tagSlug) {
    const { data: tag, error: tagError } = await supabase
      .from('tags')
      .select('id')
      .eq('slug', params.tagSlug)
      .maybeSingle()

    if (tagError) {
      throw new Error(`Failed to load tag "${params.tagSlug}": ${tagError.message}`)
    }

    if (!tag) {
      return { posts: [], totalCount: 0, page, pageSize }
    }

    const { data: postTags, error: postTagsError } = await supabase
      .from('post_tags')
      .select('post_id')
      .eq('tag_id', tag.id)

    if (postTagsError) {
      throw new Error(`Failed to load posts for tag "${params.tagSlug}": ${postTagsError.message}`)
    }

    postIdFilter = (postTags ?? []).map((row) => row.post_id)

    if (postIdFilter.length === 0) {
      return { posts: [], totalCount: 0, page, pageSize }
    }
  }

  let categoryId: string | null = null

  if (params.categorySlug) {
    const { data: category, error: categoryError } = await supabase
      .from('categories')
      .select('id')
      .eq('slug', params.categorySlug)
      .maybeSingle()

    if (categoryError) {
      throw new Error(`Failed to load category "${params.categorySlug}": ${categoryError.message}`)
    }

    if (!category) {
      return { posts: [], totalCount: 0, page, pageSize }
    }

    categoryId = category.id
  }

  function buildFilteredQuery(countMode: 'exact' | 'head') {
    let q =
      countMode === 'head'
        ? supabase
            .from('posts')
            .select('id', { count: 'exact', head: true })
            .eq('status', 'published')
        : supabase
            .from('posts')
            .select(POST_SUMMARY_COLUMNS, { count: 'exact' })
            .eq('status', 'published')
            .order('published_at', { ascending: false })

    if (categoryId) {
      q = q.eq('category_id', categoryId)
    }

    if (postIdFilter) {
      q = q.in('id', postIdFilter)
    }

    return q
  }

  const { data, error, count } = await buildFilteredQuery('exact').range(
    (page - 1) * pageSize,
    page * pageSize - 1
  )

  if (error) {
    // PostgREST retorna PGRST103 ("Requested range not satisfiable") quando
    // a página pedida está além da última linha existente para o filtro
    // aplicado. Isso não é um erro real — é só uma página vazia — então em
    // vez de derrubar a rota com 500, refazemos a mesma query filtrada (sem
    // .range()) só para contar as linhas e devolvemos uma lista vazia.
    if (error.code === 'PGRST103') {
      const { count: fallbackCount, error: countError } =
        await buildFilteredQuery('head')

      if (countError) {
        throw new Error(
          `Failed to load published posts: ${countError.message}`
        )
      }

      return {
        posts: [],
        totalCount: fallbackCount ?? 0,
        page,
        pageSize,
      }
    }

    throw new Error(`Failed to load published posts: ${error.message}`)
  }

  return {
    posts: (data ?? []).map((row) =>
      mapPostSummary(row as unknown as PostSummaryRow)
    ),
    totalCount: count ?? 0,
    page,
    pageSize,
  }
}

export async function getFeaturedPost(): Promise<PostSummary | null> {
  const supabase = await createClient()

  const { data, error } = await supabase
    .from('posts')
    .select(POST_SUMMARY_COLUMNS)
    .eq('status', 'published')
    .eq('is_featured', true)
    .order('published_at', { ascending: false })
    .limit(1)

  if (error) {
    throw new Error(`Failed to load featured post: ${error.message}`)
  }

  const row = (data ?? [])[0]

  return row ? mapPostSummary(row as unknown as PostSummaryRow) : null
}

export async function getPostBySlug(slug: string): Promise<PostDetail | null> {
  const supabase = await createClient()

  const { data, error } = await supabase
    .from('posts')
    .select(POST_DETAIL_COLUMNS)
    .eq('slug', slug)
    .eq('status', 'published')
    .maybeSingle()

  if (error) {
    throw new Error(`Failed to load post "${slug}": ${error.message}`)
  }

  return data ? mapPostDetail(data as unknown as PostDetailRow) : null
}

export async function getCategories(): Promise<Category[]> {
  const supabase = await createClient()
  const { data, error } = await supabase
    .from('categories')
    .select('id, slug, name, description')
    .order('name', { ascending: true })

  if (error) {
    throw new Error(`Failed to load categories: ${error.message}`)
  }

  return data ?? []
}

export async function getTags(): Promise<Tag[]> {
  const supabase = await createClient()
  const { data, error } = await supabase
    .from('tags')
    .select('id, slug, name')
    .order('name', { ascending: true })

  if (error) {
    throw new Error(`Failed to load tags: ${error.message}`)
  }

  return data ?? []
}

export async function getBlogSettings(): Promise<BlogSettings> {
  const supabase = await createClient()
  const { data, error } = await supabase
    .from('blog_settings')
    .select('blog_title, blog_description, default_seo_image_url')
    .eq('id', 1)
    .single()

  if (error) {
    throw new Error(`Failed to load blog settings: ${error.message}`)
  }

  return {
    blogTitle: data.blog_title,
    blogDescription: data.blog_description,
    defaultSeoImageUrl: data.default_seo_image_url,
  }
}
```

- [ ] **Step 3: Verificar**

```bash
pnpm exec tsc --noEmit && pnpm lint && pnpm build
```
Esperado: sem erros — `updatedAt` é um campo novo e obrigatório, mas todo produtor de `PostSummary`/`PostDetail` neste arquivo já foi atualizado para preenchê-lo, então nenhum consumidor existente (`PostCard`, `/blog`, `/blog/[slug]`) quebra por campo faltante.

- [ ] **Step 4: Commit**

```bash
git add src/modules/blog/types/blog.types.ts src/modules/blog/data/posts.data.ts
git commit -m "feat(blog): expor updatedAt em PostSummary"
```

---

### Task 3: `sitemap.xml` + `robots.ts`

**Files:**
- Create: `src/app/sitemap.ts`
- Create: `src/app/robots.ts`

**Interfaces:**
- Consumes: `getSiteUrl()` (Task 1), `getPublishedPosts()` (já existente, agora com `updatedAt` — Task 2).

- [ ] **Step 1: Criar `src/app/sitemap.ts`**

```typescript
import type { MetadataRoute } from 'next'
import { getPublishedPosts } from '@/modules/blog/data/posts.data'
import { getSiteUrl } from '@/lib/site-url'

export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
  const siteUrl = getSiteUrl()

  // pageSize alto o suficiente para trazer todos os posts publicados numa
  // única página — autora única, sem volume que justifique paginar o
  // sitemap nesta fase.
  const { posts } = await getPublishedPosts({ pageSize: 1000 })

  const postEntries: MetadataRoute.Sitemap = posts.map((post) => ({
    url: `${siteUrl}/blog/${post.slug}`,
    lastModified: new Date(post.updatedAt),
  }))

  return [
    {
      url: `${siteUrl}/blog`,
      lastModified: new Date(),
    },
    ...postEntries,
  ]
}
```

- [ ] **Step 2: Criar `src/app/robots.ts`**

```typescript
import type { MetadataRoute } from 'next'
import { getSiteUrl } from '@/lib/site-url'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
    },
    sitemap: `${getSiteUrl()}/sitemap.xml`,
  }
}
```

- [ ] **Step 3: Verificar**

```bash
pnpm exec tsc --noEmit && pnpm lint && pnpm build
```

```bash
pnpm dev &
sleep 4
curl -s http://localhost:3000/sitemap.xml | head -20
curl -s http://localhost:3000/robots.txt
kill %1
```
Esperado: build limpo, com `/sitemap.xml` e `/robots.txt` listados como rotas geradas; `sitemap.xml` retorna XML válido com `<url>` para `/blog` e para cada post publicado do seed; `robots.txt` retorna `User-Agent: *`, `Allow: /` e a linha `Sitemap: http://localhost:3000/sitemap.xml`.

- [ ] **Step 4: Commit**

```bash
git add src/app/sitemap.ts src/app/robots.ts
git commit -m "feat(seo): sitemap.xml e robots.ts"
```

---

### Task 4: Fallback de OG image + JSON-LD em `/blog/[slug]`

**Files:**
- Modify: `src/app/blog/page.tsx` (substituição completa do arquivo)
- Modify: `src/app/blog/[slug]/page.tsx` (substituição completa do arquivo)

**Interfaces:**
- Consumes: `getSiteUrl()` (Task 1), `PostDetail.updatedAt` (Task 2), `getBlogSettings()`/`getPostBySlug()` (já existentes).

- [ ] **Step 1: Substituir `src/app/blog/page.tsx` inteiro (só a função `generateMetadata` muda — resto idêntico)**

```tsx
import type { Metadata } from 'next'
import Link from 'next/link'
import { BlogNav } from '@/modules/blog/components/blog-nav'
import { PostList } from '@/modules/blog/components/post-list'
import { PostCard } from '@/modules/blog/components/post-card'
import { CategoryBadge } from '@/modules/blog/components/category-badge'
import { TagBadge } from '@/modules/blog/components/tag-badge'
import {
  getBlogSettings,
  getCategories,
  getFeaturedPost,
  getPublishedPosts,
  getTags,
} from '@/modules/blog/data/posts.data'

const PAGE_SIZE = 9

export async function generateMetadata(): Promise<Metadata> {
  const settings = await getBlogSettings()
  return {
    title: settings.blogTitle,
    description: settings.blogDescription ?? undefined,
    openGraph: {
      title: settings.blogTitle,
      description: settings.blogDescription ?? undefined,
      images: settings.defaultSeoImageUrl ? [settings.defaultSeoImageUrl] : undefined,
    },
  }
}

function buildPageHref(page: number, categorySlug?: string, tagSlug?: string) {
  const qs = new URLSearchParams()
  if (categorySlug) qs.set('categoria', categorySlug)
  if (tagSlug) qs.set('tag', tagSlug)
  qs.set('pagina', String(page))
  return `/blog?${qs.toString()}`
}

export default async function BlogPage({
  searchParams,
}: {
  searchParams: Promise<{ [key: string]: string | string[] | undefined }>
}) {
  const params = await searchParams
  const categorySlug =
    typeof params.categoria === 'string' ? params.categoria : undefined
  const tagSlug = typeof params.tag === 'string' ? params.tag : undefined
  const parsedPage =
    typeof params.pagina === 'string' ? Number(params.pagina) : NaN
  const requestedPage =
    Number.isFinite(parsedPage) && parsedPage >= 1 ? Math.floor(parsedPage) : 1

  const isFirstUnfilteredPage =
    requestedPage === 1 && !categorySlug && !tagSlug

  const [settings, categories, tags, { posts, totalCount, page, pageSize }, fetchedFeaturedPost] =
    await Promise.all([
      getBlogSettings(),
      getCategories(),
      getTags(),
      getPublishedPosts({
        categorySlug,
        tagSlug,
        page: requestedPage,
        pageSize: PAGE_SIZE,
      }),
      isFirstUnfilteredPage ? getFeaturedPost() : Promise.resolve(null),
    ])

  const totalPages = Math.max(1, Math.ceil(totalCount / pageSize))
  const featuredPost =
    isFirstUnfilteredPage && fetchedFeaturedPost ? fetchedFeaturedPost : undefined
  const remainingPosts = featuredPost
    ? posts.filter((post) => post.id !== featuredPost.id)
    : posts

  return (
    <div className="min-h-screen bg-[#F3E6D3] text-[#291F1A]">
      <BlogNav blogTitle={settings.blogTitle} />
      <main className="mx-auto max-w-6xl px-4 py-12 sm:px-6 sm:py-16">
        <header className="mb-10 flex flex-col gap-3">
          <h1 className="font-display text-3xl font-bold tracking-tight sm:text-4xl">
            {settings.blogTitle}
          </h1>
          {settings.blogDescription && (
            <p className="max-w-2xl text-[#6B5B52]">
              {settings.blogDescription}
            </p>
          )}
        </header>

        {(categories.length > 0 || tags.length > 0) && (
          <div className="mb-10 flex flex-wrap items-center gap-2">
            {categories.map((category) => (
              <Link key={category.id} href={`/blog?categoria=${category.slug}`}>
                <CategoryBadge category={category} />
              </Link>
            ))}
            {tags.map((tag) => (
              <Link key={tag.id} href={`/blog?tag=${tag.slug}`}>
                <TagBadge tag={tag} />
              </Link>
            ))}
            {(categorySlug || tagSlug) && (
              <Link
                href="/blog"
                className="text-xs font-medium text-[#C35A38] underline underline-offset-2"
              >
                Limpar filtro
              </Link>
            )}
          </div>
        )}

        {featuredPost && (
          <section className="mb-12">
            <div className="grid grid-cols-1">
              <PostCard post={featuredPost} />
            </div>
          </section>
        )}

        {(!featuredPost || remainingPosts.length > 0) && (
          <PostList posts={remainingPosts} />
        )}

        {totalPages > 1 && (
          <nav className="mt-12 flex items-center justify-center gap-4 text-sm font-medium">
            {page > 1 && (
              <Link
                href={buildPageHref(page - 1, categorySlug, tagSlug)}
                className="rounded-full border border-[#E0CEB7] px-4 py-2 text-[#291F1A] transition-colors hover:border-[#C35A38] hover:text-[#C35A38]"
              >
                ← Anterior
              </Link>
            )}
            <span className="text-[#6B5B52]">
              Página {page} de {totalPages}
            </span>
            {page < totalPages && (
              <Link
                href={buildPageHref(page + 1, categorySlug, tagSlug)}
                className="rounded-full border border-[#E0CEB7] px-4 py-2 text-[#291F1A] transition-colors hover:border-[#C35A38] hover:text-[#C35A38]"
              >
                Próxima →
              </Link>
            )}
          </nav>
        )}
      </main>
    </div>
  )
}
```

- [ ] **Step 2: Substituir `src/app/blog/[slug]/page.tsx` inteiro**

```tsx
import type { Metadata } from 'next'
import { notFound } from 'next/navigation'
import { BlogNav } from '@/modules/blog/components/blog-nav'
import { CategoryBadge } from '@/modules/blog/components/category-badge'
import { TagBadge } from '@/modules/blog/components/tag-badge'
import { PostBody } from '@/modules/blog/components/post-body'
import { getBlogSettings, getPostBySlug } from '@/modules/blog/data/posts.data'
import { getSiteUrl } from '@/lib/site-url'

export async function generateMetadata({
  params,
}: {
  params: Promise<{ slug: string }>
}): Promise<Metadata> {
  const { slug } = await params
  const [post, settings] = await Promise.all([
    getPostBySlug(slug),
    getBlogSettings(),
  ])

  if (!post) {
    return { title: 'Post não encontrado' }
  }

  const ogImage = post.coverImageUrl ?? settings.defaultSeoImageUrl

  return {
    title: post.seoTitle ?? post.title,
    description: post.seoDescription ?? post.excerpt,
    openGraph: {
      title: post.seoTitle ?? post.title,
      description: post.seoDescription ?? post.excerpt,
      images: ogImage ? [ogImage] : undefined,
    },
  }
}

export default async function BlogPostPage({
  params,
}: {
  params: Promise<{ slug: string }>
}) {
  const { slug } = await params
  const [post, settings] = await Promise.all([
    getPostBySlug(slug),
    getBlogSettings(),
  ])

  if (!post) {
    notFound()
  }

  const jsonLd = {
    '@context': 'https://schema.org',
    '@type': 'BlogPosting',
    headline: post.title,
    description: post.seoDescription ?? post.excerpt,
    image: post.coverImageUrl ?? settings.defaultSeoImageUrl ?? undefined,
    datePublished: post.publishedAt ?? undefined,
    dateModified: post.updatedAt,
    author: {
      '@type': 'Person',
      name: 'Íris',
    },
    url: `${getSiteUrl()}/blog/${post.slug}`,
  }

  return (
    <div className="min-h-screen bg-[#F3E6D3] text-[#291F1A]">
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
      />
      <BlogNav blogTitle={settings.blogTitle} />
      <main className="mx-auto max-w-3xl px-4 py-12 sm:px-6 sm:py-16">
        <article>
          <header className="mb-8 flex flex-col gap-4">
            <div className="flex flex-wrap items-center gap-2">
              {post.category && <CategoryBadge category={post.category} />}
              {post.tags.map((tag) => (
                <TagBadge key={tag.id} tag={tag} />
              ))}
            </div>
            <h1 className="font-display text-3xl font-bold tracking-tight sm:text-4xl">
              {post.title}
            </h1>
            {post.publishedAt && (
              <time dateTime={post.publishedAt} className="text-sm text-[#6B5B52]">
                {new Date(post.publishedAt).toLocaleDateString('pt-BR', {
                  day: '2-digit',
                  month: 'long',
                  year: 'numeric',
                })}
              </time>
            )}
            {post.coverImageUrl && (
              // eslint-disable-next-line @next/next/no-img-element
              <img
                src={post.coverImageUrl}
                alt={post.title}
                className="mt-2 aspect-[16/9] w-full rounded-2xl object-cover"
              />
            )}
          </header>
          <PostBody html={post.content} />
        </article>
      </main>
    </div>
  )
}
```

Nota: a capa acima ainda usa `<img>` — a Task 5 substitui este mesmo arquivo de novo, trocando por `next/image`. É intencional: esta task só resolve OG/JSON-LD, sem misturar as duas preocupações no mesmo diff.

- [ ] **Step 3: Verificar**

```bash
pnpm exec tsc --noEmit && pnpm lint && pnpm build
```

```bash
pnpm dev &
sleep 4
curl -s http://localhost:3000/blog/<slug-de-um-post-do-seed> | grep -o '<script type="application/ld+json">.*</script>'
kill %1
```
Esperado: build limpo; o `curl` mostra a tag `<script type="application/ld+json">` com o JSON do `BlogPosting` (substitua `<slug-de-um-post-do-seed>` por um slug real do seed de desenvolvimento da Fase 1). O Open Graph com fallback de imagem só é visualmente checável com uma ferramenta de preview de link (ex: colar a URL num validador de OG) — fica para a checagem manual final, junto com o que já ficou pendente das fases anteriores.

- [ ] **Step 4: Commit**

```bash
git add src/app/blog/page.tsx "src/app/blog/[slug]/page.tsx"
git commit -m "feat(seo): fallback de imagem de OG e JSON-LD (BlogPosting) em /blog/[slug]"
```

---

### Task 5: `next/image` nas páginas públicas do blog

**Files:**
- Modify: `next.config.ts`
- Modify: `src/modules/blog/components/post-card.tsx` (substituição completa do arquivo)
- Modify: `src/app/blog/[slug]/page.tsx` (substituição completa do arquivo)

**Interfaces:**
- Consumes: nada novo.

- [ ] **Step 1: Substituir `next.config.ts` inteiro**

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'ptwvuamkioywkqdhzqcz.supabase.co',
        pathname: '/storage/v1/object/public/blog-media/**',
      },
    ],
  },
};

export default nextConfig;
```

- [ ] **Step 2: Substituir `src/modules/blog/components/post-card.tsx` inteiro**

```tsx
import Image from 'next/image'
import Link from 'next/link'
import type { PostSummary } from '@/modules/blog/types/blog.types'
import { CategoryBadge } from './category-badge'
import { TagBadge } from './tag-badge'

export function PostCard({ post }: { post: PostSummary }) {
  return (
    <article className="flex flex-col overflow-hidden rounded-2xl border border-[#E0CEB7] bg-[#FCF7F0] shadow-sm transition-shadow hover:shadow-md">
      {post.coverImageUrl && (
        <Link
          href={`/blog/${post.slug}`}
          className="relative block aspect-[16/9] overflow-hidden"
        >
          <Image
            src={post.coverImageUrl}
            alt={post.title}
            fill
            sizes="(min-width: 1024px) 33vw, (min-width: 640px) 50vw, 100vw"
            className="object-cover transition-transform duration-300 hover:scale-105"
          />
        </Link>
      )}
      <div className="flex flex-1 flex-col gap-3 p-5 sm:p-6">
        <div className="flex flex-wrap items-center gap-2">
          {post.category && <CategoryBadge category={post.category} />}
          {post.isFeatured && (
            <span className="rounded-full bg-[#F5D98C] px-3 py-1 text-xs font-semibold uppercase tracking-wide text-[#291F1A]">
              Destaque
            </span>
          )}
        </div>
        <h2 className="font-display text-xl font-bold tracking-tight text-[#291F1A]">
          <Link
            href={`/blog/${post.slug}`}
            className="transition-colors hover:text-[#C35A38]"
          >
            {post.title}
          </Link>
        </h2>
        <p className="text-sm text-[#6B5B52]">{post.excerpt}</p>
        {post.tags.length > 0 && (
          <div className="flex flex-wrap gap-2 pt-1">
            {post.tags.map((tag) => (
              <TagBadge key={tag.id} tag={tag} />
            ))}
          </div>
        )}
      </div>
    </article>
  )
}
```

- [ ] **Step 3: Substituir `src/app/blog/[slug]/page.tsx` inteiro (só a capa muda — resto idêntico à Task 4)**

```tsx
import type { Metadata } from 'next'
import Image from 'next/image'
import { notFound } from 'next/navigation'
import { BlogNav } from '@/modules/blog/components/blog-nav'
import { CategoryBadge } from '@/modules/blog/components/category-badge'
import { TagBadge } from '@/modules/blog/components/tag-badge'
import { PostBody } from '@/modules/blog/components/post-body'
import { getBlogSettings, getPostBySlug } from '@/modules/blog/data/posts.data'
import { getSiteUrl } from '@/lib/site-url'

export async function generateMetadata({
  params,
}: {
  params: Promise<{ slug: string }>
}): Promise<Metadata> {
  const { slug } = await params
  const [post, settings] = await Promise.all([
    getPostBySlug(slug),
    getBlogSettings(),
  ])

  if (!post) {
    return { title: 'Post não encontrado' }
  }

  const ogImage = post.coverImageUrl ?? settings.defaultSeoImageUrl

  return {
    title: post.seoTitle ?? post.title,
    description: post.seoDescription ?? post.excerpt,
    openGraph: {
      title: post.seoTitle ?? post.title,
      description: post.seoDescription ?? post.excerpt,
      images: ogImage ? [ogImage] : undefined,
    },
  }
}

export default async function BlogPostPage({
  params,
}: {
  params: Promise<{ slug: string }>
}) {
  const { slug } = await params
  const [post, settings] = await Promise.all([
    getPostBySlug(slug),
    getBlogSettings(),
  ])

  if (!post) {
    notFound()
  }

  const jsonLd = {
    '@context': 'https://schema.org',
    '@type': 'BlogPosting',
    headline: post.title,
    description: post.seoDescription ?? post.excerpt,
    image: post.coverImageUrl ?? settings.defaultSeoImageUrl ?? undefined,
    datePublished: post.publishedAt ?? undefined,
    dateModified: post.updatedAt,
    author: {
      '@type': 'Person',
      name: 'Íris',
    },
    url: `${getSiteUrl()}/blog/${post.slug}`,
  }

  return (
    <div className="min-h-screen bg-[#F3E6D3] text-[#291F1A]">
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
      />
      <BlogNav blogTitle={settings.blogTitle} />
      <main className="mx-auto max-w-3xl px-4 py-12 sm:px-6 sm:py-16">
        <article>
          <header className="mb-8 flex flex-col gap-4">
            <div className="flex flex-wrap items-center gap-2">
              {post.category && <CategoryBadge category={post.category} />}
              {post.tags.map((tag) => (
                <TagBadge key={tag.id} tag={tag} />
              ))}
            </div>
            <h1 className="font-display text-3xl font-bold tracking-tight sm:text-4xl">
              {post.title}
            </h1>
            {post.publishedAt && (
              <time dateTime={post.publishedAt} className="text-sm text-[#6B5B52]">
                {new Date(post.publishedAt).toLocaleDateString('pt-BR', {
                  day: '2-digit',
                  month: 'long',
                  year: 'numeric',
                })}
              </time>
            )}
            {post.coverImageUrl && (
              <div className="relative mt-2 aspect-[16/9] w-full overflow-hidden rounded-2xl">
                <Image
                  src={post.coverImageUrl}
                  alt={post.title}
                  fill
                  sizes="(min-width: 640px) 768px, 100vw"
                  className="object-cover"
                  priority
                />
              </div>
            )}
          </header>
          <PostBody html={post.content} />
        </article>
      </main>
    </div>
  )
}
```

- [ ] **Step 4: Verificar**

```bash
pnpm exec tsc --noEmit && pnpm lint && pnpm build
```

```bash
pnpm dev &
sleep 4
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/blog
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/blog/<slug-de-um-post-do-seed>
kill %1
```
Esperado: build limpo; ambas as rotas retornam `200`. `next/image` exige que o domínio esteja em `remotePatterns` — se o build ou o carregamento da imagem falhar com erro de hostname não permitido, confirme que `NEXT_PUBLIC_SUPABASE_URL` em `.env.local` aponta para `ptwvuamkioywkqdhzqcz.supabase.co` (mesmo projeto usado em `remotePatterns`); se for um projeto Supabase diferente, ajuste o `hostname` de acordo. A renderização visual (imagem carregando de fato, sem distorção) só é verificável no navegador — fica para a checagem manual final.

- [ ] **Step 5: Commit**

```bash
git add next.config.ts src/modules/blog/components/post-card.tsx "src/app/blog/[slug]/page.tsx"
git commit -m "feat(blog): next/image nas páginas públicas (PostCard e capa do post)"
```

---

### Task 6: `loading.tsx` e `not-found.tsx` do blog público

**Files:**
- Create: `src/app/blog/loading.tsx`
- Create: `src/app/blog/[slug]/loading.tsx`
- Create: `src/app/blog/[slug]/not-found.tsx`

**Interfaces:**
- Consumes: nada — componentes puramente visuais, sem fetch de dados (convenção do Next: `loading.tsx`/`not-found.tsx` não recebem props).

- [ ] **Step 1: Criar `src/app/blog/loading.tsx`**

```tsx
export default function BlogLoading() {
  return (
    <div className="min-h-screen bg-[#F3E6D3] text-[#291F1A]">
      <div className="sticky top-0 z-50 border-b border-[#E0CEB7] bg-[#F3E6D3]/95 backdrop-blur-md">
        <div className="mx-auto flex max-w-6xl items-center justify-between gap-3 px-4 py-3.5 sm:px-6 sm:py-4">
          <div className="h-7 w-32 animate-pulse rounded bg-[#E0CEB7]" />
          <div className="h-5 w-40 animate-pulse rounded bg-[#E0CEB7]" />
        </div>
      </div>
      <main className="mx-auto max-w-6xl px-4 py-12 sm:px-6 sm:py-16">
        <div className="mb-10 flex flex-col gap-3">
          <div className="h-9 w-64 animate-pulse rounded bg-[#E0CEB7]" />
          <div className="h-5 w-96 max-w-full animate-pulse rounded bg-[#E0CEB7]" />
        </div>
        <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
          {Array.from({ length: 6 }).map((_, index) => (
            <div
              key={index}
              className="flex flex-col overflow-hidden rounded-2xl border border-[#E0CEB7] bg-[#FCF7F0]"
            >
              <div className="aspect-[16/9] animate-pulse bg-[#E0CEB7]" />
              <div className="flex flex-col gap-3 p-5 sm:p-6">
                <div className="h-4 w-20 animate-pulse rounded bg-[#E0CEB7]" />
                <div className="h-6 w-full animate-pulse rounded bg-[#E0CEB7]" />
                <div className="h-4 w-full animate-pulse rounded bg-[#E0CEB7]" />
                <div className="h-4 w-2/3 animate-pulse rounded bg-[#E0CEB7]" />
              </div>
            </div>
          ))}
        </div>
      </main>
    </div>
  )
}
```

- [ ] **Step 2: Criar `src/app/blog/[slug]/loading.tsx`**

```tsx
export default function BlogPostLoading() {
  return (
    <div className="min-h-screen bg-[#F3E6D3] text-[#291F1A]">
      <div className="sticky top-0 z-50 border-b border-[#E0CEB7] bg-[#F3E6D3]/95 backdrop-blur-md">
        <div className="mx-auto flex max-w-6xl items-center justify-between gap-3 px-4 py-3.5 sm:px-6 sm:py-4">
          <div className="h-7 w-32 animate-pulse rounded bg-[#E0CEB7]" />
          <div className="h-5 w-40 animate-pulse rounded bg-[#E0CEB7]" />
        </div>
      </div>
      <main className="mx-auto max-w-3xl px-4 py-12 sm:px-6 sm:py-16">
        <div className="mb-8 flex flex-col gap-4">
          <div className="h-6 w-24 animate-pulse rounded-full bg-[#E0CEB7]" />
          <div className="h-10 w-full animate-pulse rounded bg-[#E0CEB7]" />
          <div className="h-4 w-32 animate-pulse rounded bg-[#E0CEB7]" />
          <div className="aspect-[16/9] w-full animate-pulse rounded-2xl bg-[#E0CEB7]" />
        </div>
        <div className="flex flex-col gap-3">
          {Array.from({ length: 6 }).map((_, index) => (
            <div key={index} className="h-4 w-full animate-pulse rounded bg-[#E0CEB7]" />
          ))}
        </div>
      </main>
    </div>
  )
}
```

- [ ] **Step 3: Criar `src/app/blog/[slug]/not-found.tsx`**

```tsx
import Link from 'next/link'

export default function BlogPostNotFound() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center gap-4 bg-[#F3E6D3] px-4 text-center text-[#291F1A]">
      <h1 className="font-display text-3xl font-bold tracking-tight">
        Post não encontrado
      </h1>
      <p className="text-[#6B5B52]">
        Esse post não existe ou ainda não foi publicado.
      </p>
      <Link
        href="/blog"
        className="rounded-full border border-[#E0CEB7] px-5 py-2 text-sm font-medium text-[#291F1A] transition-colors hover:border-[#C35A38] hover:text-[#C35A38]"
      >
        ← Voltar para o blog
      </Link>
    </div>
  )
}
```

- [ ] **Step 4: Verificar**

```bash
pnpm exec tsc --noEmit && pnpm lint && pnpm build
```

```bash
pnpm dev &
sleep 4
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/blog/slug-que-nao-existe-123
kill %1
```
Esperado: build limpo; a rota de slug inexistente retorna `404` (o Next já chama `notFound()` no `page.tsx` — agora com a UI customizada em vez do 404 padrão). O conteúdo visual do `not-found.tsx` e o skeleton de `loading.tsx` (que só aparece durante o carregamento, dificilmente capturável via `curl`) ficam para a checagem manual final.

- [ ] **Step 5: Commit**

```bash
git add src/app/blog/loading.tsx "src/app/blog/[slug]/loading.tsx" "src/app/blog/[slug]/not-found.tsx"
git commit -m "feat(blog): loading.tsx e not-found.tsx nas páginas públicas"
```

---

### Task 7: `loading.tsx` e `not-found.tsx` do admin

**Files:**
- Create: `src/modules/admin/components/table-skeleton.tsx`
- Create: `src/app/admin/(protected)/posts/loading.tsx`
- Create: `src/app/admin/(protected)/categorias/loading.tsx`
- Create: `src/app/admin/(protected)/tags/loading.tsx`
- Create: `src/app/admin/(protected)/midia/loading.tsx`
- Create: `src/app/admin/(protected)/configuracoes/loading.tsx`
- Create: `src/app/admin/(protected)/posts/[id]/editar/not-found.tsx`

**Interfaces:**
- Produces: `TableSkeleton` (props: `columns: number`, `rows?: number`) — reaproveitado pelos `loading.tsx` de posts, categorias e tags (as três telas que renderizam `<table>`; mídia e configurações têm formato próprio — grade de imagens e formulário — e não se beneficiam do mesmo componente).

- [ ] **Step 1: Criar `src/modules/admin/components/table-skeleton.tsx`**

```tsx
export function TableSkeleton({
  columns,
  rows = 5,
}: {
  columns: number
  rows?: number
}) {
  return (
    <table className="w-full border-collapse text-sm">
      <tbody>
        {Array.from({ length: rows }).map((_, rowIndex) => (
          <tr key={rowIndex} className="border-b border-gray-100">
            {Array.from({ length: columns }).map((_, colIndex) => (
              <td key={colIndex} className="py-3 pr-4">
                <div className="h-4 w-full max-w-40 animate-pulse rounded bg-gray-200" />
              </td>
            ))}
          </tr>
        ))}
      </tbody>
    </table>
  )
}
```

- [ ] **Step 2: Criar `src/app/admin/(protected)/posts/loading.tsx`**

```tsx
import { TableSkeleton } from '@/modules/admin/components/table-skeleton'

export default function AdminPostsLoading() {
  return (
    <div className="flex flex-col gap-6">
      <div className="flex items-center justify-between">
        <div className="h-8 w-24 animate-pulse rounded bg-gray-200" />
        <div className="h-9 w-28 animate-pulse rounded-md bg-gray-200" />
      </div>
      <TableSkeleton columns={6} />
    </div>
  )
}
```

- [ ] **Step 3: Criar `src/app/admin/(protected)/categorias/loading.tsx`**

```tsx
import { TableSkeleton } from '@/modules/admin/components/table-skeleton'

export default function AdminCategoriesLoading() {
  return (
    <div className="flex flex-col gap-6">
      <div className="flex items-center justify-between">
        <div className="h-8 w-32 animate-pulse rounded bg-gray-200" />
        <div className="h-9 w-32 animate-pulse rounded-md bg-gray-200" />
      </div>
      <TableSkeleton columns={5} />
    </div>
  )
}
```

- [ ] **Step 4: Criar `src/app/admin/(protected)/tags/loading.tsx`**

```tsx
import { TableSkeleton } from '@/modules/admin/components/table-skeleton'

export default function AdminTagsLoading() {
  return (
    <div className="flex flex-col gap-6">
      <div className="flex items-center justify-between">
        <div className="h-8 w-20 animate-pulse rounded bg-gray-200" />
        <div className="h-9 w-24 animate-pulse rounded-md bg-gray-200" />
      </div>
      <TableSkeleton columns={4} />
    </div>
  )
}
```

- [ ] **Step 5: Criar `src/app/admin/(protected)/midia/loading.tsx`**

```tsx
export default function AdminMediaLoading() {
  return (
    <div className="flex flex-col gap-6">
      <div className="h-8 w-20 animate-pulse rounded bg-gray-200" />
      <div className="h-24 w-full animate-pulse rounded-lg bg-gray-100" />
      <div className="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4">
        {Array.from({ length: 8 }).map((_, index) => (
          <div
            key={index}
            className="flex flex-col gap-2 rounded-lg border border-gray-200 p-3"
          >
            <div className="h-32 w-full animate-pulse rounded-md bg-gray-200" />
            <div className="h-6 w-full animate-pulse rounded bg-gray-100" />
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 6: Criar `src/app/admin/(protected)/configuracoes/loading.tsx`**

```tsx
export default function AdminSettingsLoading() {
  return (
    <div className="flex flex-col gap-6">
      <div className="h-8 w-56 animate-pulse rounded bg-gray-200" />
      <div className="flex max-w-xl flex-col gap-6">
        <div className="flex flex-col gap-1">
          <div className="h-4 w-24 animate-pulse rounded bg-gray-100" />
          <div className="h-9 w-full animate-pulse rounded-md bg-gray-200" />
        </div>
        <div className="flex flex-col gap-1">
          <div className="h-4 w-32 animate-pulse rounded bg-gray-100" />
          <div className="h-20 w-full animate-pulse rounded-md bg-gray-200" />
        </div>
        <div className="flex flex-col gap-1">
          <div className="h-4 w-40 animate-pulse rounded bg-gray-100" />
          <div className="h-32 w-full max-w-xs animate-pulse rounded-md bg-gray-200" />
        </div>
        <div className="h-9 w-24 animate-pulse rounded-md bg-gray-200" />
      </div>
    </div>
  )
}
```

- [ ] **Step 7: Criar `src/app/admin/(protected)/posts/[id]/editar/not-found.tsx`**

```tsx
import Link from 'next/link'

export default function EditPostNotFound() {
  return (
    <div className="flex flex-col items-center justify-center gap-4 py-24 text-center text-gray-900">
      <h1 className="text-2xl font-semibold">Post não encontrado</h1>
      <p className="text-sm text-gray-500">
        Esse post não existe ou foi excluído.
      </p>
      <Link
        href="/admin/posts"
        className="rounded-md bg-gray-900 px-4 py-2 text-sm font-medium text-white hover:bg-gray-800"
      >
        ← Voltar para posts
      </Link>
    </div>
  )
}
```

- [ ] **Step 8: Verificar**

```bash
pnpm exec tsc --noEmit && pnpm lint && pnpm build
```

```bash
pnpm dev &
sleep 4
curl -s -o /dev/null -D - http://localhost:3000/admin/posts | grep -i "^location:"
curl -s -o /dev/null -D - http://localhost:3000/admin/posts/id-que-nao-existe/editar | grep -i "^location:"
kill %1
```
Esperado: build limpo; ambas as rotas continuam redirecionando para `/admin/login` sem sessão (confirma que os `loading.tsx`/`not-found.tsx` novos não quebraram o layout protegido). O conteúdo visual dos skeletons e do `not-found.tsx` de edição de post (que só renderiza para um admin autenticado tentando editar um post inexistente) fica para a checagem manual final.

- [ ] **Step 9: Commit**

```bash
git add src/modules/admin/components/table-skeleton.tsx "src/app/admin/(protected)/posts/loading.tsx" "src/app/admin/(protected)/categorias/loading.tsx" "src/app/admin/(protected)/tags/loading.tsx" "src/app/admin/(protected)/midia/loading.tsx" "src/app/admin/(protected)/configuracoes/loading.tsx" "src/app/admin/(protected)/posts/[id]/editar/not-found.tsx"
git commit -m "feat(admin): loading.tsx e not-found.tsx nas telas do admin"
```

---

### Task 8: Checklist de deploy

**Files:**
- Create: `docs/deploy-checklist.md`

**Interfaces:** nenhuma — documentação, sem código.

- [ ] **Step 1: Criar `docs/deploy-checklist.md`**

```markdown
# Checklist de deploy — Blog Íris

## Variáveis de ambiente de produção

Configurar no provedor de hosting (ex: Vercel → Project Settings →
Environment Variables), nunca commitadas:

- `NEXT_PUBLIC_SUPABASE_URL` — URL do projeto Supabase
  (`https://ptwvuamkioywkqdhzqcz.supabase.co` neste projeto, ou o
  equivalente se migrar para outro projeto Supabase).
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` — chave anônima (`anon`/`public`) do
  mesmo projeto — painel do Supabase em Project Settings → API.
- `ADMIN_USER_ID` — UUID do usuário único do admin no Supabase Auth (ver
  seção "Provisionamento do usuário admin" abaixo).
- `NEXT_PUBLIC_SITE_URL` — URL de produção do site (ex:
  `https://blog.iris.com.br`), **sem barra final**. Usada para montar URLs
  absolutas em `sitemap.xml`, `robots.txt`, Open Graph e JSON-LD.

## CORS do bucket de Storage (`blog-media`)

O bucket `blog-media` serve as imagens de capa e da biblioteca de mídia
diretamente do domínio do Supabase Storage (não passa pelo servidor
Next.js) — confirmar que o domínio de produção consegue carregá-las:

1. No painel do Supabase → Storage → Configuration, confirmar que o bucket
   está marcado como público (necessário para a leitura via URL direta já
   usada desde a Fase 1).
2. Testar, já em produção, que uma URL de imagem do bucket
   (`https://<ref>.supabase.co/storage/v1/object/public/blog-media/...`)
   carrega sem erro de CORS a partir do domínio de produção — abrir uma
   página do blog publicado e inspecionar a aba Network do navegador.
3. Se houver erro de CORS: o Supabase Storage por padrão libera qualquer
   origem para buckets públicos via `SELECT` (a policy já criada na Fase 1
   não restringe por domínio) — um erro aqui é mais provável de ser
   `next.config.ts` (`images.remotePatterns`, ver Task 5 deste plano) do
   que CORS do Supabase propriamente dito. Confirmar as duas coisas.

## Provisionamento do usuário admin

Passo a passo já executado manualmente na Fase 2a, documentado aqui para
reprodutibilidade (recriar em um ambiente novo — staging, por exemplo, ou
se o projeto Supabase precisar ser recriado):

1. No painel do Supabase → Authentication → Users → "Add user" → "Create
   new user".
2. Preencher e-mail e senha da Íris. Marcar "Auto Confirm User" (não há
   fluxo de confirmação por e-mail nesta aplicação — login único, sem
   cadastro público).
3. Copiar o UUID do usuário criado (coluna "UID" na lista de usuários) e
   definir como `ADMIN_USER_ID` no ambiente (local e produção).
4. Confirmar login em `/admin/login` com o e-mail/senha criados.

## Segurança do Supabase Auth

Achado da auditoria de RLS feita durante o brainstorming desta fase (ver
`docs/superpowers/specs/2026-09-04-blog-fase3-design.md`, seção "Auditoria
de RLS"): **proteção contra senha vazada está desabilitada.**

1. No painel do Supabase → Authentication → Policies (ou Auth → Providers,
   dependendo da versão do painel), habilitar "Leaked password protection".
2. Não afeta o login já existente — só passa a impedir a troca para uma
   senha presente em vazamentos conhecidos (HaveIBeenPwned) daqui pra
   frente.

## RLS — confirmação final

A auditoria completa (políticas de `posts`, `categories`, `tags`,
`post_tags`, `blog_settings`, `media_assets` e do bucket `blog-media`) já
foi feita e documentada em
`docs/superpowers/specs/2026-09-04-blog-fase3-design.md` — sem
divergências do que a Fase 1 especificou. Nenhuma ação de código pendente
aqui; esta seção existe só para apontar onde a evidência está, caso o
checklist seja usado independentemente da spec no futuro.
```

- [ ] **Step 2: Verificar**

```bash
pnpm exec tsc --noEmit && pnpm lint && pnpm build
```
Esperado: sem erros — este passo não toca em nenhum arquivo de código, a verificação aqui só confirma que nada mais no repo quebrou. O conteúdo do checklist em si (CORS, criação de usuário, proteção de senha vazada) é manual, executado fora deste repositório (painel do Supabase, provedor de hosting) e não tem verificação automatizável — registrado explicitamente na spec e neste plano.

- [ ] **Step 3: Commit**

```bash
git add docs/deploy-checklist.md
git commit -m "docs: checklist de deploy da Fase 3"
```
